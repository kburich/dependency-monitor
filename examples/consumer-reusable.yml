# Copy this into your repo as .github/workflows/dependency-monitor.yml
#
# Secrets to configure in your repo:
#   RL_TOKEN        — Spectra Assure Community token (free: https://secure.software/)
#   HEARTBEAT_URL   — optional healthchecks.io-style ping URL

name: Dependency monitor

on:
  schedule:
    - cron: "0 6 * * *"   # daily or twice-daily is plenty
  workflow_dispatch: {}    # manual runs + keeps the cron from looking abandoned

jobs:
  monitor:
    uses: kburich/dependency-monitor/.github/workflows/monitor.yml@v4
    permissions:
      contents: write
      issues: write
    # The baseline is kept on its own orphan branch by default, so nothing
    # here needs branch protection to be relaxed. To commit it into the repo
    # itself instead — visible in PR diffs, but the default branch must accept
    # direct pushes:
    # with:
    #   baseline-branch: ""
    secrets:
      rl-token: ${{ secrets.RL_TOKEN }}
      heartbeat-url: ${{ secrets.HEARTBEAT_URL }}
